# Environment variables
# TEMPORARY - replace with real JWT auth in Phase 9, do not ship to production without this replacement.
NEXT_PUBLIC_ADMIN_TEMP_PASSWORD=

# MongoDB connection string (Railway). Do not commit the real value.
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/shapewear-closet
